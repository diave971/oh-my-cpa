# Dependencies
node_modules/
.pnpm-store/

# Generated from @lobehub/icons-static-svg by scripts/sync-lobe-icons.mjs
web/public/lobe-icons/

# Production builds
web/dist/
dist/
internal/web/dist/*
!internal/web/dist/
!internal/web/dist/index.html

# TypeScript build info
*.tsbuildinfo

# OS metadata
.DS_Store
Thumbs.db

# IDE, editor, and local agent artifacts
.idea/
.vscode/
.pi/
*.local

# Go build artifacts
bin/
tmp/
# Scratch with the `tmp-` prefix rather than the directory: agent and review tooling writes
# both, and one of them reached master because this line named only the directory.
tmp-*

# Runtime data
/data/
*.db
*.db-shm
*.db-wal
oh-my-cpa-data/
/cpa/
caddy-data/
caddy-config/

# Local debugging: real CPA binary/config and dotenv
.env
!.env.example

# Air hot-reload artifacts
.air.toml.bak
# Local version-pinned tool cache
.tools/

# Fixture binaries built by the acceptance harness. Anchored to the repo root:
# a bare name would also match scripts/fixture/seed-usage/, which is source.
/seed-usage
/seed-usage.exe

# Vercel's local state: the project link, which holds the project and team
# identifiers, and the OIDC file the CLI writes for `vercel dev`. Both are
# per-machine deployment state rather than anything the repository needs. The OIDC
# file is named explicitly rather than matched as `.env*`, which would also hide the
# example file that has to stay tracked.
.vercel/
.vercel
.env.local
